{
  "name": "@unipass/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Client-side helpers for interacting with the UniPass Registry",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "prettier --check \"src/**/*.ts\"",
    "test": "echo \"No SDK tests yet\""
  },
  "dependencies": {
    "@semaphore-protocol/identity": "^4.14.0",
    "@semaphore-protocol/utils": "^4.14.0",
    "ethers": "^6.13.2",
    "poseidon-lite": "^0.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
